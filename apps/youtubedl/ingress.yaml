apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: youtubedl-ingress
  namespace: youtubedl
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - host: youtubedl.apps.rke2.koperwas.eu.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: youtubedl
                port:
                  number: 80                                                      